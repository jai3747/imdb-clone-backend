# # values-prod.yaml
global:
  namespace: prod
  environment: production
backend:
  replicaCount: 1
  image:
    repository: jai3747/imdb-clone-backend-prod
    tag: latest
    pullPolicy: Always
    mongoUrl: "mongodb+srv://JAYACHANDRAN:KQJrxDn44181NsqT@cluster0.w45he.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0" # Replace with your MongoDB URL

  service:
    type: ClusterIP
    port: 5000
  domain: "prod.backend.jayachandran.xyz"
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "200m"
  probes:
    readiness:
      initialDelaySeconds: 20
      periodSeconds: 20
    liveness:
      initialDelaySeconds: 25
      periodSeconds: 30
frontend:
  replicaCount: 1
  image:
    repository: jai3747/imdb-clone-frontend-prod
    tag: latest
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 3000
    targetPort: 3000
  domain: "prod.frontend.jayachandran.xyz"
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "200m"
  probes:
    readiness:
      initialDelaySeconds: 20
      periodSeconds: 20
    liveness:
      initialDelaySeconds: 25
      periodSeconds: 30

ingress:
  sslRedirect: true
  frontendDomain: "prod.frontend.jayachandran.xyz"
  backendDomain: "prod.backend.jayachandran.xyz"
